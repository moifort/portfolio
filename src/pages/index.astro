---
import { Image } from 'astro:assets'
import portrait from '../assets/portrait.jpg'
import appIcon from '../assets/vinarium/app-icon.png'
import dashboardShot from '../assets/vinarium/dashboard.png'
import CryptoPhoneScreen from '../components/CryptoPhoneScreen.astro'
import IPhoneFrame from '../components/IPhoneFrame.astro'
import Reveal from '../components/Reveal.astro'
import SiteFooter from '../components/SiteFooter.astro'
import StatCounter from '../components/StatCounter.astro'
import { cryptoBot, sinceLabel } from '../data/crypto-bot'
import { site } from '../data/site'
import Base from '../layouts/Base.astro'

const method = [
  {
    n: '01',
    title: 'Vision & domain',
    text: 'Name the concepts, draw the boundaries. A domain model everyone — human or machine — can reason about.',
  },
  {
    n: '02',
    title: 'Architecture as guardrails',
    text: 'FP core, branded types, errors as values. Types so strict the LLM cannot drift outside the design.',
  },
  {
    n: '03',
    title: 'Supervised build loops',
    text: 'Claude writes, I review. Thin vertical slices, precise briefs, ruthless code review — shipped daily.',
  },
  {
    n: '04',
    title: 'Production, for real',
    text: 'Real users, real money, monitoring, forward-only migrations. Products that survive contact with reality.',
  },
]

const stack = [
  'TypeScript',
  'Swift · SwiftUI',
  'GraphQL · Pothos',
  'Nitro · Bun',
  'Firebase · GCP',
  'Firestore',
  'Terraform',
  'Docker',
  'Claude API',
  'Gemini API',
]
---

<Base
  title="Thibaut Mottet — I don't write code anymore. I ship products."
  description="Senior full-stack developer turned LLM supervisor. Architecture, FP, DDD and product method — Claude writes the code. Real products, used every day."
  theme="home"
>
  <!-- Act 1 · Hero -->
  <header class="hero">
    <div class="container hero__inner">
      <div class="hero__text">
        <Reveal>
          <p class="eyebrow">{site.name} · {site.role} · {site.location}</p>
        </Reveal>
        <Reveal delay={0.08}>
          <h1 class="display">
            I don't write code anymore.<br />I ship products.
          </h1>
        </Reveal>
        <Reveal delay={0.15}>
          <p class="lede hero__lede">
            LLMs flipped software development on its head. I supply the architecture, the method and
            the taste — Claude supplies the code. The products below are the proof.
          </p>
        </Reveal>
        <Reveal delay={0.2}>
          <div class="hero__actions">
            <a class="btn btn--primary" href="#proof">See the proof</a>
            <a class="btn btn--ghost" href={site.github} rel="noopener">GitHub</a>
          </div>
        </Reveal>
      </div>
      <Reveal delay={0.15} class="hero__portrait-wrap">
        <Image
          class="hero__portrait"
          src={portrait}
          alt="Portrait of Thibaut Mottet"
          width={460}
          height={460}
          loading="eager"
          fetchpriority="high"
        />
      </Reveal>
    </div>
  </header>

  <!-- Act 2 · The shift -->
  <section class="section">
    <div class="container--narrow">
      <Reveal><p class="eyebrow">The paradigm shift</p></Reveal>
      <Reveal delay={0.05}>
        <h2 class="headline shift__headline">
          The value of a senior developer used to be syntax. Not anymore.
        </h2>
      </Reveal>
      <Reveal group class="shift__copy">
        <p>
          For most of my career, seniority meant mastering languages down to their darkest corners.
          That knowledge was the moat — and models like Claude drained it almost overnight.
        </p>
        <p>
          The bottleneck is no longer <em>writing</em> code. It's knowing what to build, how to
          shape it, and how to tell good from bad — fast. So I stopped memorizing syntax and
          doubled down on what doesn't commoditize: <strong
            >architecture, method, and product judgment.</strong
          >
        </p>
        <p>
          Today I work as a tech lead for machines. I set the constraints, review everything, and
          ship every day.
        </p>
      </Reveal>
    </div>
  </section>

  <!-- Act 3 · How I work now -->
  <section class="section">
    <div class="container">
      <Reveal><p class="eyebrow">How I work now</p></Reveal>
      <Reveal delay={0.05}><h2 class="headline">What I actually bring</h2></Reveal>
      <Reveal group class="how__grid">
        <article class="card how__card">
          <h3>Architect's vision</h3>
          <p>
            Functional programming, DDD &amp; CQRS, branded types, errors as discriminated unions.
            I draw the boundaries of the system — the LLM fills them in, and can't drift outside.
          </p>
        </article>
        <article class="card how__card">
          <h3>Product method</h3>
          <p>
            From vision to domain model to thin vertical slices. Ship something usable early, then
            compound. A product you use every day beats a perfect backlog.
          </p>
        </article>
        <article class="card how__card">
          <h3>LLM supervision</h3>
          <p>
            I direct Claude the way a lead directs a team: precise briefs, hard constraints,
            ruthless reviews, tight feedback loops. Output is only as good as its supervision.
          </p>
        </article>
      </Reveal>
    </div>
  </section>

  <!-- Act 3b · The method, in practice -->
  <section class="section">
    <div class="container">
      <Reveal><p class="eyebrow">The method, in practice</p></Reveal>
      <Reveal delay={0.05}>
        <h2 class="headline">From idea to production, every time</h2>
      </Reveal>
      <Reveal group class="method__grid">
        {
          method.map((step) => (
            <article class="method__step">
              <span class="method__n">{step.n}</span>
              <h3>{step.title}</h3>
              <p>{step.text}</p>
            </article>
          ))
        }
      </Reveal>
      <Reveal class="stack">
        <p class="stack__label">Supervised daily, across the whole stack:</p>
        <ul class="stack__list">
          {stack.map((tech) => <li>{tech}</li>)}
        </ul>
      </Reveal>
    </div>
  </section>

  <!-- Act 4 · Proof -->
  <section class="section" id="proof">
    <div class="container">
      <Reveal><p class="eyebrow">The proof</p></Reveal>
      <Reveal delay={0.05}>
        <h2 class="headline">Two real products.<br />Built with Claude. Used every day.</h2>
      </Reveal>
      <div class="proof__grid">
        <Reveal>
          <a class="proof-card proof-card--crypto" href="/crypto-bot">
            <div class="proof-card__body">
              <p class="proof-card__eyebrow">CryptoBot · Grid trading, 24/7</p>
              <p class="proof-card__stat">
                <StatCounter value={cryptoBot.netPercent} decimals={2} prefix="+" suffix="%" />
              </p>
              <p class="proof-card__caption">
                net, on real money — while Bitcoin dropped {Math.abs(cryptoBot.holdPercent)}%
              </p>
              <ul class="proof-card__facts">
                <li><strong>+{cryptoBot.alphaVsHold} pts</strong> alpha vs HOLD</li>
                <li><strong>{cryptoBot.trades}</strong> trades since {sinceLabel}</li>
                <li><strong>{cryptoBot.cycleSeconds}s</strong> cycle on {cryptoBot.exchange}</li>
              </ul>
              <span class="proof-card__cta">See it work →</span>
            </div>
            <div class="proof-card__phone" aria-hidden="true">
              <IPhoneFrame>
                <CryptoPhoneScreen />
              </IPhoneFrame>
            </div>
          </a>
        </Reveal>
        <Reveal delay={0.1}>
          <a class="proof-card proof-card--vinarium" href="/vinarium">
            <div class="proof-card__body">
              <Image
                class="proof-card__icon"
                src={appIcon}
                alt="Vinarium app icon"
                width={200}
                height={200}
              />
              <p class="proof-card__eyebrow">Vinarium · iOS</p>
              <p class="proof-card__title">A wine cellar that knows itself.</p>
              <p class="proof-card__caption">
                AI label scanning, live cellar value, drink windows.
              </p>
              <ul class="proof-card__facts">
                <li><strong>Claude</strong> reads the labels</li>
                <li><strong>Gemini</strong> estimates the price</li>
                <li><strong>SwiftUI</strong> native, iOS 26</li>
              </ul>
              <span class="proof-card__cta">Take the tour →</span>
            </div>
            <div class="proof-card__phone" aria-hidden="true">
              <IPhoneFrame src={dashboardShot} alt="" />
            </div>
          </a>
        </Reveal>
      </div>
    </div>
  </section>

  <!-- Act 5 · Contact -->
  <section class="section contact">
    <div class="container--narrow contact__inner">
      <Reveal>
        <h2 class="headline">Looking for someone who ships?</h2>
      </Reveal>
      <Reveal delay={0.1}>
        <p class="lede">
          I'm open to senior roles where architecture, product sense and AI leverage matter more
          than typing speed.
        </p>
      </Reveal>
      <Reveal delay={0.2}>
        <div class="hero__actions contact__actions">
          <a class="btn btn--primary" href={`mailto:${site.email}`}>Get in touch</a>
          <a class="btn btn--ghost" href={site.github} rel="noopener">GitHub</a>
        </div>
      </Reveal>
    </div>
  </section>

  <SiteFooter />
</Base>

<style>
  .hero {
    min-height: 92svh;
    display: grid;
    align-items: center;
    padding-block: 4rem;
    position: relative;
    overflow: clip;
    background:
      radial-gradient(55% 45% at 88% 12%, rgba(48, 209, 88, 0.09) 0%, transparent 65%),
      radial-gradient(45% 40% at 8% 88%, rgba(114, 47, 55, 0.09) 0%, transparent 65%),
      var(--bg);
  }

  .hero__inner {
    display: grid;
    grid-template-columns: 1.35fr 0.65fr;
    align-items: center;
    gap: clamp(2rem, 6vw, 5rem);
  }

  .hero__text {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.75rem;
  }

  :global(.hero__portrait-wrap) {
    justify-self: center;
  }

  :global(.hero__portrait) {
    width: clamp(13rem, 22vw, 19rem);
    height: auto;
    border-radius: 2rem;
    box-shadow: 0 40px 80px -30px rgba(22, 24, 29, 0.4);
    rotate: 2deg;
  }

  @media (max-width: 820px) {
    .hero__inner {
      grid-template-columns: 1fr;
      justify-items: center;
      text-align: center;
    }

    .hero__text {
      align-items: center;
      order: 2;
    }

    :global(.hero__portrait-wrap) {
      order: 1;
    }

    :global(.hero__portrait) {
      width: 11rem;
      border-radius: 1.5rem;
    }
  }

  .hero__lede {
    max-width: 38rem;
  }

  .hero__actions {
    display: flex;
    gap: 0.9rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .shift__headline {
    margin-block: 1rem 2.25rem;
  }

  /* :global — these classes land on component-rendered elements (Reveal/Image),
     which don't receive this page's scope hash */
  :global(.shift__copy) {
    display: flex;
    flex-direction: column;
    gap: 1.4rem;
    font-size: clamp(1.0625rem, 1.6vw, 1.25rem);
    line-height: 1.7;
  }

  :global(.shift__copy) em {
    font-style: italic;
  }

  :global(.how__grid) {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(16rem, 1fr));
    gap: 1.25rem;
    margin-top: 2.5rem;
  }

  .how__card {
    padding: 2rem;
  }

  .how__card h3 {
    font-size: 1.25rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    margin-bottom: 0.75rem;
  }

  .how__card p {
    color: var(--muted);
    font-size: 1rem;
  }

  /* Method */
  :global(.method__grid) {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
    gap: 1.25rem;
    margin-top: 2.5rem;
  }

  .method__step {
    padding: 1.75rem;
    border-left: 2px solid var(--border);
  }

  .method__n {
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--muted);
    letter-spacing: 0.1em;
  }

  .method__step h3 {
    font-size: 1.15rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    margin-block: 0.6rem 0.5rem;
  }

  .method__step p {
    color: var(--muted);
    font-size: 0.9688rem;
    line-height: 1.6;
  }

  :global(.stack) {
    margin-top: 3.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .stack__label {
    color: var(--muted);
    font-size: 0.9375rem;
  }

  .stack__list {
    list-style: none;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .stack__list li {
    padding: 0.5rem 1.1rem;
    border-radius: 999px;
    border: 1px solid var(--border);
    background: var(--card);
    font-size: 0.9375rem;
    font-weight: 550;
  }

  /* Proof */
  .proof__grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(19rem, 1fr));
    gap: 1.25rem;
    margin-top: 2.5rem;
  }

  .proof-card {
    position: relative;
    display: block;
    min-height: 28rem;
    padding: 2.25rem;
    border-radius: var(--radius-card);
    text-decoration: none;
    transition: transform 0.35s var(--ease-out), box-shadow 0.35s var(--ease-out);
    overflow: hidden;
  }

  .proof-card__body {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.6rem;
    height: 100%;
    max-width: 62%;
  }

  .proof-card__facts {
    list-style: none;
    padding: 0;
    margin-top: 0.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    font-size: 0.9375rem;
    opacity: 0.75;
  }

  .proof-card__facts strong {
    font-weight: 750;
  }

  .proof-card__phone {
    position: absolute;
    right: -3.5rem;
    top: 3.5rem;
    rotate: 8deg;
    pointer-events: none;
  }

  .proof-card__phone :global(.iphone) {
    --iphone-w: clamp(180px, 16vw, 230px);
  }

  @media (max-width: 640px) {
    .proof-card__body {
      max-width: 100%;
    }

    .proof-card__phone {
      position: static;
      rotate: 0deg;
      margin-top: 1.5rem;
      display: flex;
      justify-content: center;
    }

    .proof-card {
      min-height: 0;
    }
  }

  .proof-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 30px 60px -24px rgba(0, 0, 0, 0.35);
  }

  .proof-card--crypto {
    background: radial-gradient(120% 120% at 80% 0%, #14231a 0%, #0a0a0f 55%);
    color: #f5f5f7;
    justify-content: center;
  }

  .proof-card__stat {
    font-size: clamp(3.5rem, 7vw, 5.5rem);
    font-weight: 850;
    letter-spacing: -0.04em;
    line-height: 1;
    color: var(--green);
    font-variant-numeric: tabular-nums;
  }

  .proof-card--vinarium {
    background: radial-gradient(120% 120% at 20% 0%, #2c1a22 0%, #181114 60%);
    color: #f6efe7;
    justify-content: center;
  }

  :global(.proof-card__icon) {
    width: 4.5rem;
    height: 4.5rem;
    border-radius: 1.05rem;
    box-shadow: 0 10px 24px -8px rgba(0, 0, 0, 0.5);
    margin-bottom: 0.5rem;
  }

  .proof-card__eyebrow {
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    opacity: 0.6;
  }

  .proof-card__title {
    font-size: clamp(1.6rem, 3vw, 2.2rem);
    font-weight: 750;
    letter-spacing: -0.02em;
    line-height: 1.15;
  }

  .proof-card__caption {
    opacity: 0.65;
    font-size: 1rem;
  }

  .proof-card__cta {
    margin-top: auto;
    padding-top: 1.5rem;
    font-weight: 650;
  }

  .contact__inner {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
  }
</style>
