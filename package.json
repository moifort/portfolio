{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "My rad Nuxt.js project",
  "author": "Thibaut Mottet",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "export NODE_ENV=PROD && yarn generate && yarn firebase deploy --only hosting"
  },
  "dependencies": {
    "@nuxtjs/google-analytics": "^2.2.0",
    "@nuxtjs/pwa": "^3.0.0-0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.0.0",
    "firebase-tools": "^7.3.2"
  }
}
